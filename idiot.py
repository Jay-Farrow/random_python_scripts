# Don't name a file the same as another module
# Example: you named a file random.py, which altered the random module when imported for use.
# The imported module was the empty random.py you created. 